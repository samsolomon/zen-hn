{
  "name": "zen-hn",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts --bundle --outfile=dist/icons.js --format=iife && esbuild src/utils.ts --bundle --outfile=dist/utils.js --format=iife && esbuild src/pages.ts --bundle --outfile=dist/pages.js --format=iife && esbuild src/submissionMenu.ts --bundle --outfile=dist/submissionMenu.js --format=iife && esbuild src/random.ts --bundle --outfile=dist/random.js --format=iife && esbuild src/commentCollapse.ts --bundle --outfile=dist/commentCollapse.js --format=iife && esbuild src/favorites.ts --bundle --outfile=dist/favorites.js --format=iife && esbuild src/replyForm.ts --bundle --outfile=dist/replyForm.js --format=iife && cp logic.js dist/logic.js && cp content.js dist/content.js && cp src/colors.css dist/colors.css && cp styles.css dist/styles.css",
    "test": "node --test tests/*.test.js && node --import tsx --test tests/*.test.ts",
    "test:js": "node --test tests/*.test.js",
    "test:ts": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
